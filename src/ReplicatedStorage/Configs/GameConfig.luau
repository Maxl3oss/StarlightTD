-- GameConfig.luau
-- Shared config accessible by both Client and Server

local GameConfig = {
	-- UI
	EnableLoadingScreen = false,
	BaseMaxHealth = 100,
	StartingCoins = 300,
	VictoryCashReward = 100,
}

return GameConfig
