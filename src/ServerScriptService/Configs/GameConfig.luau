-- GameConfig.luau
-- Server-only config for sensitive game parameters

local GameConfig = {
	-- UI
	EnableLoadingScreen = false,
	BaseMaxHealth = 100,
	StartingCoins = 300,
	VictoryCashReward = 100,
}

return GameConfig
